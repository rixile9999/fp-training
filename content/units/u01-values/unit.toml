# 유닛 메타 — fpdojo/content/schema.UnitMeta 와 필드 1:1 (PLAN §3.1 U1, §5.3).
# 태그는 core/types.tag_key 직렬 표기("concept:<slug>")로 참조하며
# 전부 content/registry/tags.toml 에 등록되어 있어야 한다(빌드 검증).

id = "u01-values"
title = "값, 불변성, 표현식"
order = 1
level = 1

# 이 유닛이 다루는 개념 태그 (Exercism slug 정렬: let/불변성/표현식 -> basics,
# Int/Float 분리 -> ints, floats — curriculum.md §3 개념 매핑표)
concepts = ["concept:basics", "concept:ints", "concept:floats"]

# U1은 커리큘럼의 뿌리 — 선수 유닛 없음 (PLAN §3.1).
prerequisites = []

# TODO: 정식 U1은 레슨 5개 (curriculum.md U1 ①~⑤: 값과 let / 불변성과
#       shadowing / Int와 Float는 남남 / 모든 것이 표현식 / String과 Bool, echo).
#       현재는 스키마 시연용 1개만 저작됨.
lesson_ids = ["l01-let-and-immutability"]
